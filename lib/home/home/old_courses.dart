import 'package:flutter/material.dart';

class OldCourses extends StatelessWidget {
  const OldCourses({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Old Course'),
    );
  }
}
